# redesign/PLAN.md — ui-redesign-flow worklist

> Single source of truth for resume. Rows update **in place** — never append a duplicate row.
> `done` rows are immutable (forward-only). Flags: `stitch:fallback`, `tokens:drift`, `collides:<slug>`.
> Status: `todo | in-progress | awaiting-signoff | done | skipped | blocked`.

| slug | route | components | stage | status | flags | progress |
|------|-------|-----------|-------|--------|-------|----------|
| producto-detalle | /productos/[id] | components/products/ProductDetailPageClient.tsx (+Gallery, SellerCard, OwnerListingNotice, ProductActions, ShareButton) | 8-verify | done | signoff 2026-09-13; impl 205d5cf; tokens:drift (resuelto vía clases compartidas); g6:preexisting-debt (decisión humana 2026-09-13; test a11y de la página PASS; deuda → buscar-storefront-a11y) | 8/8 |
| buscar-a11y | /buscar | app/buscar/page.tsx (violaciones axe viven en markup inline; hijos SearchBar/SearchFilters/ProductGrid fuera de scope) | 8-verify | done | signoff 2026-09-15 (iteración 2); impl e9b8d35+dc7de61; stitch:fallback (loop 1: mock Stitch descartado por divergencia — mockup fiel sobre DOM real); tokens:drift (advisory en propuesta; resuelto en impl — solo tokens); g6:preexisting-debt (resto de suite = /publicar timeout axe mobile, fuera de scope → publicar-a11y; storefront pasó esta corrida) | 8/8 |
| storefront-a11y | /negocio/[slug] | app/negocio/[slug]/page.tsx + components/business/BusinessInfoSidebar.tsx (violación axe vive ahí; BusinessHeader/ProductGrid fuera de scope) + tests/helpers/index.ts (slug test) | 8-verify | done | signoff 2026-09-15 (iteración 1, hash 8243f471…); impl 6dd37b4; stitch:fallback (mockup fiel, precedente buscar loop 1); g6:preexisting-debt (único BLOCK = /publicar mobile timeout axe, 3ª corrida consistente, fuera de scope → publicar-a11y; storefront test PASÓ ambos viewports); specs m4.5 slug stale = deferred (aprobado) | 8/8 |
| publicar-a11y | /publicar + /busco/publicar | components/products/ProductFormWizard.tsx (F-1/F-4) + app/busco/publicar/page.tsx (F-2) + app/publicar/page.tsx (sin cambios propios, par de referencia) + tests/e2e/cross-cutting/accessibility-audit.spec.ts (F-3) | 8-verify | done | signoff 2026-09-15 (hash 17fa7f96…); impl cb7de56; stitch:fallback (mockup fiel, precedente buscar/storefront); tokens:drift (advisory en propuesta); **gates 20/20 PASS — G6 incluido** (F-3 test.slow() eliminó el timeout crónico; deuda G6 de los 3 items previos saldada) | 8/8 |
| busco-publicar-ui | /busco/publicar | app/busco/publicar/page.tsx + components/demand/DemandPostForm.tsx (wizard demanda completo: stepper, 4 pasos, revisión) | done | — | item UI (visual/UX, no solo a11y — publicar-a11y ya cerró F-1..F-4); stitch:fallback (mockup fiel, precedente buscar/storefront/publicar); F-1..F-6 (P1 voseo unificación + guard variantes voseo, P2 heading, P2 banner borrador→inline, P2 paso 2 chips, P2 paso 4 stack+stats+checklist, P2 touch targets patrón hermano); a11y explícitamente fuera de alcance (baseline publicar-a11y 0 violaciones) | 8/8 gates 20/20 PASS; upgrade híbrido negocios/personas (migración 20260916130000, edge BUSINESS/PROFILE, lib query-embedding) |
| unified-search | /buscar + Header | components/search/SearchBar.tsx + components/layout/Header.tsx + app/buscar/page.tsx (scope.txt crece con migración RPCs + rutas API + SearchTypeTabs/BusinessResultCard/PersonResultCard + SearchFilters/SearchSort + spec e2e antes de Stage 8) | done | after/ capturado (3 viewports × resultados+vacío+header); AUDIT-RESOLUTION 5/5 resolved; signoff 2026-09-16 (iteración 1: fix solape chip selector — chip hijo directo del form, no del wrapper); stitch:fallback (mockup fiel, precedente buscar/storefront/publicar/busco) | búsqueda unificada productos+negocios+solicitudes+personas; selector tipo en header `[Todo ▾][input][Buscar]` + tabs en /buscar; decisión humana 2026-09-16 (alcance 4 entidades, UX selector); backend nuevo (2 RPCs migración + 2 rutas API) = prerrequisito en Stage 8; privacidad: search_profiles SECURITY INVOKER + JOIN solo perfiles con productos activos, sin phone (TELO-003) | 8/8 gates 20/20 PASS; upgrade híbrido negocios/personas (migración 20260916130000, edge BUSINESS/PROFILE, lib query-embedding) |
| buscar-business-card | /buscar?type=negocios | components/search/BusinessResultCard.tsx + components/search/PersonResultCard.tsx (hermano misma familia, in-scope por consistencia G3d) + app/buscar/page.tsx (contenedor ul→grid) | 8-verify | done | signoff 2026-09-16 (iteración 0, hash 7a9017bb… con nota de transparencia por fix de vocabulario G3d post-aprobación); stitch:fallback (mockup fiel, precedente buscar/storefront/publicar/busco); tokens:drift (advisory — hex del propio comparison.html, G7b limpio) | intake = screenshot prod del usuario 2026-09-16 post-merge ("card de negocios similar a producto"); Checkpoint 1 cubierto por pedido explícito; **gates 20/20 PASS** (G6 incluido, a11y 31/31); search-discovery 36 passed |
| tinted-card-contrast-sweep | cross-cutting (layout + productos owner) | components/products/OwnerListingNotice.tsx + components/layout/MobileNavigationDrawer.tsx + components/layout/UserMenu.tsx (3 P1 muted-on-bg-primary/5 ocultos a axe) + borderline ProductFormWizard.tsx:384 / DemandPostForm.tsx:411 + drift ResilientHomePreview.tsx:116 / app/busco/page.tsx:411 | 8-done | done | origen: ops note 2026-09-15; decisión humana 2026-09-17; sign-off aprobado 2026-09-17 (sha256 d82a96d3…fec126); ALL GATES PASS 19/19 mismo día; branch redesign/tinted-card-contrast-sweep listo para merge humano | 8/8 |

## Ops notes

- 2026-09-12: preflight — 0 blocks. WARNs: dev server off (Stage 3 lo arranca), Supabase off (ruta pública, no requerido), STITCH_API_KEY heredada posiblemente stale (re-exportar de ~/.config/environment.d/stitch.conf antes de Stage 6), sin estado logged-in (no requerido, ruta pública). Primer item real: `producto-detalle` (input del usuario: screenshot products/4jZfwwvcTUi14sEdNtYH58--horquilla-massi). Deuda a11y conocida en /buscar + storefront (main) — fuera de scope de este item.
- 2026-09-13: Stage 7 sign-off registrado (SIGNOFF.md, approver Alcides, hash content-only G4b `4ab3e703…`). Nota: primera corrida gates.sh — G4b BLOCK stale porque el hash inicial se calculó con filenames; corregido con fórmula content-only del gate. gates.sh además cuelga al final si `npm run dev` queda como job hijo (bash do_wait) — matar proceso tras leer GATES.log; la próxima corrida completa ocurre en Stage 8. G6 BLOCK a11y pre-impl esperado (deuda /buscar+storefront en main). Avance a Stage 8.
- 2026-09-13: Stage 8 implementado en branch `redesign/producto-detalle` (commit `205d5cf`, 4 archivos en scope). G8a PASS (after-captures 3 viewports), G8b PASS (6/6 findings resolved), G7/G7b PASS (scope+tokens limpios). Único BLOCK: G6 — fallas son exclusivamente la deuda pre-existente `/buscar` contrast ×2 + storefront timeout (test "Product detail page" de esta página PASS). Item NO marcado done (disciplina fail-closed): pendiente decisión humana — marcar done con flag documentado, o item propio de deuda a11y primero. Reorg de Documentation/ se commiteó en main (`4c9749f`) con ok del usuario para desbloquear G7.
- 2026-09-15: preflight — 0 blocks nuevos. 2 reconcile BLOCKs esperados y resueltos/documentados: (1) dir `buscar-storefront-a11y` faltante → creada al iniciar intake (inicio normal de item, no repair); (2) `producto-detalle` sin G6 PASS en GATES.log → caso documentado 2026-09-13 (flag g6:preexisting-debt, deuda = este item; fila done inmutable). WARNs: dev server off → arrancado (ok), Supabase off → arrancado (ok, datos seeded: samsung ×2 + storefront tienda-electronica-la-paz), STITCH_API_KEY posiblemente stale (re-exportar antes de Stage 6), sin logged-in state (rutas públicas, no requerido). Stage 1 intake: capturas desktop de `/buscar?q=samsung`, `/buscar?q=zzzznoexiste` (empty state) y `/negocio/tienda-electronica-la-paz` en `input/`. Checkpoint 1 resuelto 2026-09-15: usuario eligió SEPARAR en dos items — `buscar-a11y` (contraste ×2, in-progress con capturas propias) y `storefront-a11y` (timeout, todo, secuenciado después); dir renombrada en consecuencia.
- 2026-09-15 (mismo día, continuado): Stages 2-6 completados para `buscar-a11y`. Stage 2: IDENTIFICATION.md + landmark ("Buscar Productos") + scope (`app/buscar/page.tsx`) + PATTERN-INSTANCES.md (2 in-scope, 5 not-affected). Axe scan directo pineó las 2 violaciones: `text-muted-foreground` (#737373) sobre `bg-primary/5` (#f3f3f3) = 4.27:1, en CTA cards de page.tsx:354 y :402. HALLAZGO LATERAL para `storefront-a11y`: `/negocio/usuario-de-desarrollo` (TEST_DATA.businessSlug) da **404** hoy — el test de storefront de la suite a11y probablemente audita una página 404; investigar slug stale vs seed antes de Stage 4 de ese item. Stage 3: current/ con 6 capturas (3 viewports × resultados/empty), fullPage dsf2, HTTP 200, landmark en dom.txt. Stage 4: AUDIT.md F-1..F-5 (2×P1 contraste, P2 amber hardcode, P2 doble CTA, P2 contraste al límite). Stage 5: stitch-brief.md. Stage 6: ruta Stitch OK (projects/2913740147222886002, screen 186cc24c…, stitch-meta.json grabado) — render 3 viewports a proposal/stitch-render-*.png (nota: tablet renderiza 797px CSS por overflow del mock, documentado); comparison.html con pares antes/después + tablas G3c/G3d + propuesta embebida. Nota transparencia: mock Stitch rediseña header/cards/footer FUERA de scope — Stage 8 solo toca page.tsx (F-1..F-5). Gates certificados en corrida completa de Stage 8. Pendiente: Checkpoint 3 (sign-off humano).
- 2026-09-15 (iteración 1 del Checkpoint 3): usuario RECHAZA mock Stitch — "no hay consistencia con el tema de la app actual, lo veo diferente". Rerun Stage 6 por ruta fallback (flag stitch:fallback): mockup.html = DOM real hidratado de /buscar (scripts removidos, CSS built inline) + parche mínimo — F-1 text-foreground/80 (≈8.6:1, mismo tratamiento que hermanas), F-2/F-4/F-5 empty consolidado en una card con fila border-t, F-3 banner offline neutro inyectado (demo). proposal/stitch.html + renders Stitch eliminados; proyecto Stitch 2913740147222886002 descartado (stitch-meta.json registra el descarte para no re-spawn). comparison.html regenerado con renders del mockup (desktop/tablet/mobile + par empty). Defecto de render declarado: imágenes de producto en blanco en after (lazy-load sin JS). Pendiente: re-presentar a Checkpoint 3.
- 2026-09-15 (iteración 2 del Checkpoint 3): usuario pide oportunidades de mejora; acordado doblar 3 mejoras baratas en page.tsx a la propuesta: (1) remover "Mostrando N de M" redundante, (2) omitir "No hay más productos." si totalCount < PAGE_SIZE, (3) banner offline con botón Reintentar funcional (cablear a performSearch en Stage 8). Diferidos a items futuros: CTA demanda como botón sólido, sugerencias en empty state, help-text de SearchFilters, filtro route-scoped para G6. Brief + stitch-meta actualizados (loops 1-2), renders + comparison regenerados. Stage 8 debe verificar contraste del fix en tema OSCURO además del claro. Pendiente: re-presentar a Checkpoint 3.
- 2026-09-15 (Stage 8 buscar-a11y): sign-off aprobado (iteración 2) → branch `redesign/buscar-a11y`, impl `e9b8d35` (F-1..F-5 + 3 mejoras en app/buscar/page.tsx) + `dc7de61` (artefactos verificación). Correcciones de gates en el camino: (a) G0d — current/http-status.txt estaba multi-línea, normalizado a bare `200`; (b) G3d — línea template `- instance: <repo path>` de PATTERN-INSTANCES.md se parseaba como sibling real, removida; (c) G4b — husky/prettier reformateó stitch-meta.json durante el commit → hash stale; SIGNOFF actualizado con nota de transparencia (solo formato, cero cambio de diseño). Dark mode verificado: 0 violaciones color-contrast (dark-mode-axe.txt; resto: landmark-unique en SearchBar, fuera de scope). After-captures 6/6 (3 viewports × resultados/empty). Gates finales: 18/19 PASS — G8b PASS, único BLOCK G6: suite a11y full-repo con 2 fallas AMBAS fuera de scope: product-detail [mobile] `document-title` (flake — pasó en retry) y publish [mobile] timeout axe analyze() 30s consistente (nuevo item `publicar-a11y`). Storefront test PASÓ esta corrida (timeout flaky, no crónico). Fila PLAN → done 8/8 con flag g6:preexisting-debt (precedente producto-detalle). Pendiente: Checkpoint 4 (merge humano).
- 2026-09-15 (post-impl bug sweep del branch buscar-a11y): implementación SIN bugs funcionales (Reintentar, condición "No hay más productos.", textos de tests preservados). Pero el sweep repo-wide del patrón `bg-primary/5 + text-muted-foreground` (misma clase P1 que F-1/F-2, 4.27:1) encontró 3 violaciones OCULTAS que axe no ve porque los contenedores están cerrados/anon en los audits — PATTERN-INSTANCES.md las había clasificado not-affected (falso negativo): (1) components/products/OwnerListingNotice.tsx:23,29,34 — 3 párrafos muted sobre Card bg-primary/5, visible solo al dueño; (2) components/layout/MobileNavigationDrawer.tsx:120-122 — muted sobre banner bg-primary/5, drawer cerrado en audits; (3) components/layout/UserMenu.tsx:100-104 — muted text-[11px] sobre grupo bg-primary/5, dropdown cerrado en audits. Riesgo F-5 (borderline ~4.6:1): ProductFormWizard.tsx:384 y DemandPostForm.tsx:411 — muted sobre bg-muted/30. Drift F-3 no registrado (banners amber, contrast OK): ResilientHomePreview.tsx:116 y app/busco/page.tsx:411. Propuesta pendiente de decisión humana: item propio "tinted-card-contrast-sweep" (o doblar en storefront-a11y intake). No se toca nada en este branch (scope cerrado).
- 2026-09-15 (Stage 8 storefront-a11y): branch `redesign/storefront-a11y`, impl `6dd37b4` (F-1 red-700, F-2 green-800, F-3 banner bg-muted/40 sin amber, F-4 slug test, F-5 text-foreground/80) + artefactos verificación. Nota de scope: border del banner quedó `border-primary/35` intacto (cambio accidental a /20 revertido — fuera de la condición del sign-off). After-captures 6/6 (3 viewports × storefront/404). Dark mode verificado: badgers estáticos (F-1 5.91:1, F-2 6.81:1 en ambos temas), banner 13.70:1, axe dark CLEAN (dark-mode-axe.txt). Defecto conocido: badge "Abierto" no renderiza hoy (lunes closed) — parche verificado en código/compilado. Gates finales: 18/19 PASS — G8a/G8b PASS; único BLOCK G6 = /publicar mobile timeout axe analyze() (3ª corrida consistente, deuda pre-existente intocada por branch → item publicar-a11y). Fila PLAN → done 8/8 con flag g6:preexisting-debt (precedente buscar-a11y/producto-detalle). Pendiente: Checkpoint 4 (merge humano).
- 2026-09-15 (inicio publicar-a11y): preflight — WARNs: Supabase corriendo (WARN falso negativo, solo imgproxy/edge/pooler stopped), STITCH_API_KEY stale (fallback disponible), logged-in state faltante → creado `tests/.auth/logged-in.json` vía login real (dev@telopillo.test). 3 reconcile BLOCKs conocidos (g6:preexisting-debt en las 3 filas done, documentados). USUARIO señala: existen DOS flujos publish — `/publicar` (producto, ProductFormWizard, el del timeout G6) y `/busco/publicar` (demanda/solicitud, DemandPostForm). Stage 1 intake: capturas desktop de ambas rutas logged-in. Decisión de scope (1 item vs 2) → Checkpoint 1.
- 2026-09-15 (publicar-a11y Stages 2-4): IDENTIFICATION + landmark ("Publicar Producto"/"Publicá lo que buscás", G0d any-match) + scope (5 archivos) + PATTERN-INSTANCES (9, edit forms heredan fixes vía wizards reutilizados). Stage 3: current/ 6 capturas (3 viewports × 2 rutas, logged-in). Stage 4: step-through axe de 20 estados (4 pasos × error states × 2 rutas × 2 viewports) = **0 violaciones**; timeout G6 root-caused como contención de suite (réplica aislada 1.6s / 0 viol). AUDIT.md F-1..F-4, todos P2: F-1 error imágenes duplicado (wizard :559+:561), F-2 demanda sin mx-auto (:79,102), F-3 fix de test timeout (spec :150), F-4 error sin role="alert". Contrastes manuales PASS (destructive 4.76, muted 4.73/4.54).
- 2026-09-15 (publicar-a11y Stages 5-6): stitch-brief.md (fallback: fixes de presencia/atributos, mock Stitch no aporta). Ruta fallback `stitch:fallback` (precedente buscar loop 1 + storefront): build-mockup.mjs genera mocks fieles sobre DOM real hidratado de ambas rutas (scripts + nextjs-portal removidos, CSS built inlineeado, parches inline: F-2 margen auto en container demanda; F-1 remoción del <p> duplicado dejando la copia #image-upload-error con role="alert" de ImageUpload). Renders 3+2 viewports (proposal/render-*.png, fullPage dsf2). comparison.html: 5 pares antes/después (demanda 3 viewports F-2 + producto error-state desktop/mobile F-1/F-4) + G3c (4/4 addressed) + G3d (9/9 instancias) + mock demanda embebido. Transparencia: barra sticky Cancelar/Siguiente se superpone en fullPage mobile — artefacto idéntico ambos lados; skeleton de carga de demanda (:79) no renderiza en mock (solo página real :102). Fix G2 en el camino: líneas evidence exigían formato `^evidence:` (sin prefijo de lista). Gates: G0a-G0d/G1/G2/G3/G3c/G3d PASS + FLAG tokens:drift (advisory — hex del propio comparison.html, no de la propuesta); BLOCKs restantes = Stage 7/8 esperados, G6 BLOCK = el propio F-3. Hash prop e7c871b7…. Pendiente: Checkpoint 3.
- 2026-09-15 (publicar-a11y Checkpoint 3 + Stage 8): usuario confirmó cobertura del step-through de 20 pasos y aprobó: "procede a implementarlo" (SIGNOFF 7b70bbe; hash corregido e7c871b7→17fa7f96 con fórmula content-only del gate, mtimes prueban cero cambio de contenido — nota en SIGNOFF). Impl `cb7de56` (F-1 remove <p> duplicado, F-2 mx-auto ×2, F-3 test.slow()). Después-capturas 6/6 + audit-after (error state: 1 línea vs 2 antes). AUDIT-RESOLUTION: 4/4 resolved, 0 open/deferred. **Gates finales 20/20 PASS — primer item con G6 incluido**: F-3 eliminó el timeout crónico que arrastraban producto-detalle/buscar-a11y/storefront-a11y como g6:preexisting-debt (deuda saldada a nivel repo). Incidente de proceso: el `git checkout -b` inicial no ejecutó (reflog sin checkout) → commits de impl quedaron en main; corregido creando branch en cb7de56 y `git reset --hard 7b70bbe` en main (main vuelve a pre-impl; sin pérdida). Pendiente: Checkpoint 4 (merge humano). ✅ merged a1e85de (human), pushed.
- 2026-09-15 (busco-publicar-ui Stages 3-6): Stage 3 current/ 6 capturas (3 viewports × paso1/paso4-revisión, logged-in, fullPage dsf=2). Stage 4 AUDIT.md F-1..F-5: **F-1 P1 voz mixta voseo/tuteo** (tuteo en DemandPostForm :525,581,649,669,691,754,830-832 + LocationSelector :75,101,112 vs voseo :547,566 + page.tsx h1 — fix: unificar a voseo); F-2 heading paso 2 casing/duplica label (:605/:636); F-3 banner borrador permanente (:409-417); F-4 paso 2 baja densidad desktop (2-col sm+ propuesto); F-5 card consejos duplica helpers → checklist de verificación (:826-834). a11y fuera de alcance explícito (baseline publicar-a11y 0 violaciones/20 estados). Stage 5 brief (constraint voseo + hermandad wizard producto). Stage 6 ruta fallback `stitch:fallback`: build-mockup.mjs = DOM real hidratado (scripts/portal stripped, CSS inlineeado) + parches inline (14 reemplazos voseo, heading/label, grid 2-col con media query inyectada — cuidado: style inline pisa stylesheet; checklist innerHTML) → mockup-paso2/paso4.html; renders 6 (2 estados × 3 viewports). comparison.html: 6 pares + G3c 5/5 addressed + G3d 10 instancias + transparencia. Hash content-only prop 2bd4fc59…. Gotchas nuevos: (1) inline style overridea la hoja inyectada — columnas base van en el <style> inyectado; (2) filtro de wrappers por classList regex, no querySelector('.space-y-2') (busca descendientes). Fila PLAN → 7-signoff awaiting-signoff. Pendiente: Checkpoint 3.
- 2026-09-15 (busco-publicar-ui Checkpoint 3 + Stage 8): Iteración 1 feedback "lo de subcategorias no me convence" (dropdown tapa columna vecina) → F-4 chips. Iteración 2 feedback "resumen y antes de publicar tambien esta raro" (sidebar flaca) → F-5 stack 1 col + stats + checklist merged. Pregunta usuario sobre consistencia vs wizard producto → F-6 promovido (touch targets min-h-[44px] sm:min-h-0). Aprobación: "implementa estos findings debe de estar consistentes". SIGNOFF e65c83c (hash content-only 9e672b5f…, G4a/G4b PASS). Impl 6474ad8 en branch redesign/busco-publicar-ui: F-1 voseo + variantes voseo en PLACEHOLDER_PATTERNS (lib/utils/demand.ts añadido a scope), F-2 heading/label, F-3 saved→inline aria-live en footer, F-4 chips aria-pressed, F-5 ChecklistMark computado, F-6 sm:min-h-0. after/ 6 capturas + desktop-paso2 extra, asserts live (heading F-2 + Verificación F-5). AUDIT-RESOLUTION 6/6 resolved. Gates finales 20/20 PASS (exit 144 = gotcha do_wait, log válido). Fila PLAN → done 8/8. Pendiente: Checkpoint 4 merge humano (triplet current/ vs proposal/ vs after/). Deuda futura: doblar F-3 a ProductFormWizard /publicar.
- 2026-09-15 (busco-publicar-ui post-done feedback Checkpoint 4): "hubicacion en busco no es consistente con producto" — LocationSelector triggers `h-11` sin `w-full` = selects angostos vs hermano producto. Fix 1aab250: `w-full min-h-[44px] sm:min-h-0` + asteriscos rojos text-destructive en labels. Header MapPin "Ubicación" del producto NO replicado (LocationSelector compartido con profile/edit + BusinessProfileForm — filtraría fuera de scope). after/desktop-paso3.png evidencia. Gates re-run 20/20 PASS. Nota: placeholder producto "Selecciona un departamento" (ProductFormWizard :787) sigue en tuteo — deuda item futuro /publicar.
- 2026-09-15 (busco-publicar-ui post-done feedback 2): "la ciudad debe de ser input text no un list drop down en solicitud" — ciudad paso 3 → input texto libre igual que producto (prop `cityInput` en LocationSelector; dropdown conservado para profile/edit + BusinessProfileForm). Zod demanda alineado a producto (max 100 + guard isPlaceholderCity exportado desde product.ts). Commit 54feeb0, scope.txt += lib/validations/{demand,product}.ts. Gates: 1er run G6 FAIL por flake documentado del test "Publish page" /publicar mobile (timeout 90s, test no tocado por este item; suite 30/31 passed); re-run 20/20 PASS.
- 2026-09-15 (busco-publicar-ui post-done feedback 3): "asimila esto a solicitud tambien, donde nos da las opciones" (+screenshot dropdown producto) — subcategoría chips → **Select dropdown idéntico a producto** (revierte decisión chips iteración 1; consistencia con producto gana). SelectTrigger `id="subcategory"` (compat tests manuales) `w-full min-h-[44px] sm:min-h-0`, placeholder voseo "Seleccioná una subcategoría" (F-1), 8 opciones verificadas idénticas en DOM vivo. after-capture.mjs actualizado (dropdown paso 2; paso 3 = 1 combobox + `#city` fill — antes asumía 2 comboboxes, roto desde feedback 2). after/ 6 capturas re-hechas. Gates re-run 19 PASS / 0 FAIL (G6 limpio, sin flake esta vez). Commit 216a068.
- 2026-09-16 (Checkpoint 4 busco-publicar-ui): merge a main + push. Pre-merge apareció trabajo huérfano de sesión anterior sin commitear ni documentar: lock síncrono anti double-submit (`submitLockRef`) en DemandPostForm + ProductFormWizard, y spec E2E `tests/e2e/cross-cutting/double-submit-guard.spec.ts` sin validar (1 error TS). Commits: 7d9a94a (lock), a39a4a1 (spec + scope.txt += ambos), 6b786db (estabilización spec: skip si banner quota 24h visible — soft-delete cuenta para el límite MAX_POSTS_PER_DAY, cleanup UI nunca libera quota; skip proyecto mobile — browser crashea en wizards pesados en dev; lock es viewport-independiente). Spec verificado: chromium 2 passed (1 server-action POST exacto en doble click, ambos wizards), mobile 2 skipped. Nota descubierta: el límite 24h de demandas cuenta posts soft-deleted (`status:'deleted'`) — posible bug/deuda futura. Gates finales re-run: 19 PASS / 0 BLOCK. Merge `52059d5` (--no-ff, precedente storefront-a11y), pushed a origin/main. Fila done inmutable.
- 2026-09-16 (unified-search Stages 7-8): Signoff Checkpoint 3 (iteración 1: solape chip selector — chip directo del form). Impl fases 1→5: migración 20260916120000 (search_vector + GIN en business_profiles, RPCs search_businesses/search_profiles SECURITY INVOKER, totals via count(*) OVER () — CREATE TEMP TABLE prohibido en STABLE; RETURNS TABLE orden columnas jsonb-primero), rutas /api/search-businesses + /api/search-profiles (keyword-only, sin phone), /buscar unificada (SearchTypeTabs tablist + contadores debounced, endpoint map, cache per-tipo, KEPT_PARAMS stripping, filtros adaptativos enabledFilters/sortOptions, BusinessResultCard/PersonResultCard con link rule SellerCard), header selector [Todo ▾] + overlay móvil reusa SearchBar (form duplicado eliminado). Fix 50e8c9e: selector default Todo sin type param + trigger aria-label case bug. Spec nueva unified-search.spec.ts 19/19; search-discovery+header-monkey+navigation-layout 110 passed; accessibility-audit 31/31; type-check clean; lint 0 errores. after/ capturado, AUDIT-RESOLUTION 5/5. Commits a2ba67f…c5bb0bf. Pendiente: gates finales + Checkpoint 4 merge humano.
- 2026-09-16 (unified-search upgrade híbrido): usuario pidió negocios/personas al nivel de productos. Migración 20260916130000 (embeddings + HNSW + triggers pg_net + RRF adaptativo k=60 en search_businesses/search_profiles; profiles.embedding expuesto vía profiles_public sin phone), edge generate-embedding modos BUSINESS/PROFILE + backfill multi-tabla, lib/search/query-embedding.ts compartida, rutas reportan searchMode hybrid|keyword. Local verificado end-to-end (flag on, backfill 78/78, triggers 200, queries semánticas reales OK). Commits a793ba4, 3091157, e8c2d24. Deploy remoto pendiente: db push + functions deploy + secrets + flag + backfill.
- 2026-09-16 (buscar-business-card Stages 1-6): intake = screenshot prod post-merge. Stage 2: IDENTIFICATION + landmark ("Negocios") + scope (3 archivos) + PATTERN-INSTANCES (2 in-scope, 4 not-affected). Stage 3: current/ 12 capturas (3 viewports × negocios/vacío/productos + desktop/mobile-personas añadidas para el par del hermano) + dom.txt + http-status 200 + manifest. Stage 4: AUDIT.md F-1..F-6 (2×P1 jerarquía/grid, 4×P2 tile/desc/badge/heading). Stage 5: brief (referencia ProductCard/ProductGrid, sin backend). Stage 6 ruta fallback: build-mockup.mjs — DOM real hidratado + cards reconstruidas con datos de la propia API (6 negocios, 16 personas reales) + ul→grid espejo ProductGrid; gotchas: clases del AvatarFallback vivo se filtran a SOLO tokens de color (size-full del shadcn pisaba el h-20 del tile), rateo de iconos lucide inline. Renders 3 viewports × 2 tabs + comparison.html (5 pares + G3c 6/6 + G3d 6 instancias + transparencia). Pendiente: Checkpoint 3.
- 2026-09-16 (buscar-business-card Checkpoint 3 + Stage 8): aprobación iteración 0 ("procede", hash 8015901f → 7a9017bb con nota: fix vocabulario G3d "in-scope"→"addressed" post-aprobación, cero cambio visual). Impl en branch redesign/buscar-business-card: rebuild BusinessResultCard/PersonResultCard tipo ProductCard con patrón overlay (Link absolute inset-0 en tile + h3 con Link propio — VerificationBadge focusable fuera del link, axe nested-interactive; declarado en SIGNOFF antes de implementar), contenedores page.tsx → grid lg:3. Correcciones de gates en el camino: (a) G3d — parser exige addressed|deferred|not-affected en la línea del path de comparison.html; (b) G8a — after/ necesita http-status.txt bare 200 + dom.txt con landmark (after-capture.mjs actualizado). Verificación: type-check clean, lint 0 errores, search-discovery 36 passed, live probe grid 3-col + 0 interactivos anidados, after/ 6 capturas + dom + status. **Gates finales 20/20 PASS (G6 incluido, a11y 31/31, exit 0 sin hang do_wait)**. Pendiente: Checkpoint 4 (merge humano). ✅ merged vía GitHub PR #2 `3e930ef` (humano), pushed; local main sincronizado 2026-09-17, branch local borrado.
- 2026-09-17 (inicio tinted-card-contrast-sweep): preflight — 3 reconcile BLOCKs conocidos (g6:preexisting-debt en filas producto-detalle/buscar-a11y/storefront-a11y done, flags documentados, deuda saldada repo-wide por publicar-a11y F-3; precedente 2026-09-15 proceder). WARNs: dev server off → arrancado background, Supabase off → arrancado background (OwnerListingNotice requiere owner logueado; logged-in state existe tests/.auth/logged-in.json), STITCH_API_KEY posible stale (re-exportar antes de Stage 6 o fallback). Item = decisión humana 2026-09-17 sobre propuesta ops note 2026-09-15. Fila creada 1-intake in-progress.
- 2026-09-17 (tinted-card-contrast-sweep stages 2-6): identificación cross-cutting 8 archivos (scope.txt) + PATTERN-INSTANCES 21 instancias (8 in-scope, 2 deferred: productPresentation metaLabel / NetworkStatusBanner; 11 not-affected). Capturas current/ 9 (3 vistas canónicas owner + usermenu + drawer×2 + drafts×3 + busco). AUDIT 5 findings (F-1/F-2/F-3 P1 4.27:1; F-4 P2 amber 4.39:1 FAIL + muted/30 4.62:1 borderline; F-5 P2 drift amber hardcodeado). Fix verificado text-foreground/80 = 17.8-19.3:1. Stage 6 fallback (stitch:fallback, precedente ×5): 5 mockups DOM-fiel + 7 renders + comparison.html 15MB (7 pares, tablas G3c/G3d, mockups embebidos srcdoc, notas transparencia demos inyectados). Gates: G3/G3c/G3d PASS, G3b advisory tokens:drift (hex en CSS inlineado de mockups — no action, mismo flag que precedentes). Gotcha G3d golpeó de nuevo: línea template `- instance: <repo path>` dentro de code fence SÍ la parsea el gate (case match) → removida y documentada en el propio archivo. Pendiente: Stage 7 sign-off humano (bloqueante).
- 2026-09-17 (tinted-card-contrast-sweep stage 7-8): Stage 7 sign-off humano APROBADO (comparison.html 7 pares + deferred explícitos; SIGNOFF.md sha256 d82a96d379f0d746c57f1ca0299d8bd96c4a180b178a8e7d26012d2564fec126, G4a/G4b PASS). Stage 8: branch desde main 3e930ef, 8 archivos editados (solo scope.txt), fix aplicado F-1..F-5 (muted→text-foreground/80 ×15 sitios; amber offline ×2 → border-border/60 bg-muted/40 + fg/80). Gates: G5a/G5b/G5c/G6/G7/G7b/G8a/G8b PASS — primer run G5a+G6 transitorios (cold-compile race: GATES_WARM_ROUTES no cubre rutas del spec a11y; re-run caliente PASS). Re-medición viva en branch: F-3 17.84:1/19.13:1; probes DOM confirman fg/80 en F-1/F-2/F-4; dark-mode axe limpio / /buscar /busco. 10 after-capturas mirror 1:1. NOTA: usuario editó components/auth/OAuthButtons.tsx (queryParams prompt=consent TESTING ONLY) en working tree a las 16:39 — stash temporal para run G7 limpio, restaurado después; cambio NO parte de este commit/branch. Pendiente humano: Checkpoint 4 (review current/proposal/after + merge).
